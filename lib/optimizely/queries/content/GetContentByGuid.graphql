query GetContentByGuid($guid: String!) {
  _Content(where: { guid: { eq: $guid } }) {
    items {
      _metadata {
        displayName
        version
        key
        url {
          base
          internal
          hierarchical
          default
          type
        }
      }
    }
  }
}
