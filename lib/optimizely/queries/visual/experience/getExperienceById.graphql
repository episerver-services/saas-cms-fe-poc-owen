query GetExperienceById($ids: [String!]!, $locale: [Locales!]) {
  _Experience(ids: $ids, locale: $locale) {
    items {
      ... on _IExperience {
        composition {
          nodes {
            ...CompositionNodeData
          }
        }
      }
    }
  }
}
